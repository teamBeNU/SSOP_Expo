import { View, Text } from "react-native";

export default function WorkerTemplate({ navigation }) {
    return (
        <View>
            <Text>WorkerTemplate</Text>
        </View>
      
    );
  }